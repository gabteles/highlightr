export type Highlight = {
  uuid?: string;
  url: string;
  createdAt: string;
  text: string;
  container: string;
  anchorNode: string,
  anchorOffset: number,
  focusNode: string,
  focusOffset: number,
};
